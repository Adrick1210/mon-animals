<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>
  <body>
    <%- include("../partials/header.ejs") %>
    <div class="container">
      <form action="/animals/<%= animal._id %>?_method=PUT" method="post">
        <h1>Edit <%= animal.species %></h1>
        <label
          >Species:
          <input type="text" name="species" placeholder="<%= animal.species %>"
        /></label>
        <label>Extinct? <input type="checkbox" name="extinct" /></label>
        <label
          >Location:
          <input
            type="text"
            name="location"
            placeholder="<%= animal.location %>"
        /></label>
        <label
          >Life Expectancy:
          <input
            type="number"
            name="lifeExpectancy"
            placeholder="<%= animal.lifeExpectancy %>"
        /></label>
        <div class="btn-container">
        <button class="btn btn-success">Update <%= animal.species %></button>
      </div>
      </form>
      <div class="btn-main">
        <a href="/animals" class="btn btn-primary" role="button"
          >Back to Main</a
        >
      </div>
    </div>
  </body>
</html>
